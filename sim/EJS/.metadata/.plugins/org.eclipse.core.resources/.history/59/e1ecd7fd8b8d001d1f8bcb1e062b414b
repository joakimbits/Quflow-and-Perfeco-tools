
import java.util.*;

/** A DrawingIconSelectionEventListener is an interface that is 
    implemented by a class that is interested in receiving
    DrawingIconSelectionEvents.  Upon the occurence of a 
    DrawingIconSelectionEvent, the iconSelected method of the
    DrawingIconSelectionEventListener is called to notify the
    listener that a drawing icon was selected. */

public interface DrawingIconSelectionEventListener extends EventListener {

  public void iconSelected (DrawingIconSelectionEvent aDrawingIconSelectionEvent);

}
